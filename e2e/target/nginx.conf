server {
  listen 443 ssl;
  ssl_certificate /etc/nginx/tls/target.crt;
  ssl_certificate_key /etc/nginx/tls/target.key;
  location / {
    default_type text/plain;
    return 200 "a page of the end to end job's target\n";
  }
}
